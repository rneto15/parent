meta {
  name: Callback
  type: http
  seq: 1
}

delete {
  url: {{endpoint}}/v2/callbacks/{{id}}
  body: multipartForm
  auth: bearer
}

headers {
  gumgaToken: {{gumgaToken}}
  Content-Type: application/json
}

auth:bearer {
  token: gumgaToken={{token}}
}
